language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-bangular' ]; then cd .. && eval "mv $currentfolder generator-bangular" && cd generator-bangular; fi

